// WORK IN PROGRESS
// This class handles the validation of the user login function in HMSApp
// It will validate user inputs, throw exceptions if there are any issues, and return error messages accordingly
class LoginCredentialsValidator implements Validator<String> {
    public void validate(String toValidate) {
        //if(toValidate == null || toValidate.isEmpty()) throw new ValidationException("empty!!!");
    }
}